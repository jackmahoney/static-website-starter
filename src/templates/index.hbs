{{> header}}
  <div class="component-fullscreen">
    <div class="container">
      <div class="row">
        <div class="col-sm-12">
          <h1 class="text-center">{{seo.title}}</h1>
        </div>
      </div>
    </div>
  </div>
{{> footer}}